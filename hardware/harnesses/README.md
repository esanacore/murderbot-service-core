# harnesses/

Empty pending Phase 5 (`../../ROADMAP.md`). Bench wiring is not yet
converted to keyed, strain-relieved harnesses — see
`../../docs/wiring.md` for the current connector inventory and its
`TBD` markers.

When harnesses are built, record here per harness: connectors on each
end (with the citation from `docs/wiring.md`), wire gauge, length,
keying/labeling scheme, and a photo reference under `docs/images/`.
