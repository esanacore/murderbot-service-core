# pcb/

Empty. Blocked on schematic freeze (`../schematics/`) and the bench
prototype proving out pin allocation — see `AGENTS.md` non-goals ("no
custom production PCB before the bench prototype and pin allocation are
proven") and the P3 backlog item in `../../docs/backlog.md`.
