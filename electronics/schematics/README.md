# schematics/

Empty. No KiCad project exists yet — bench wiring (perfboard,
point-to-point) comes first per `../../ROADMAP.md` Phases 2-4. A
schematic gets created here once a circuit is bench-proven and its pin
allocation is considered stable enough to be worth formalizing (see the
P3 backlog item in `../../docs/backlog.md`).
